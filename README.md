# Docker image for Rust on arm-linux

Contains rustup with the `arm-unknown-linux-gueabihf` and `armv7-unknown-linux-gueabihf` targets
installed and working.

Note however that this doesn't use musl.
